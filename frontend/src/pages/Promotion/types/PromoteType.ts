export type PromoteType = {
  _id: string;
  employeeName: string;
  oldPosition: string;
  newPosition: string;
  newSalary: number;
  oldSalary: number;
  trainedBy: string;
  dateOfPromotion: string;
  dateOfHire: string;
};
